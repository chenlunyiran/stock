layui.define([ 'form', 'laydate', 'table','shopTransportTemplateApi'], function(exports) {
    var form = layui.form;
    var shopTransportTemplateApi = layui.shopTransportTemplateApi;
    var index = layui.index;
    var view = {
        init:function(){
            Lib.initGenrealForm($("#addForm"),form);
            this.initSubmit();
        },
        initSubmit:function(){
            $("#addButton").click(function(){
                 form.on('submit(form)', function(){
                     shopTransportTemplateApi.addShopTransportTemplate($('#addForm'),function(){
                         parent.window.dataReload();
                         Common.info("添加成功");
                         Lib.closeFrame();
                     });
                });
            });
        
            $("#addButton-cancel").click(function(){
                Lib.closeFrame();
            });
        }
    			
    }
    exports('add',view);
});