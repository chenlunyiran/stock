layui.define([ 'form', 'laydate', 'table','shopCustomerAccountApi'], function(exports) {
    var form = layui.form;
    var shopCustomerAccountApi = layui.shopCustomerAccountApi;
    var index = layui.index;
    var view = {
        init:function(){
            Lib.initGenrealForm($("#addForm"),form);
            this.initSubmit();
        },
        initSubmit:function(){
            $("#addButton").click(function(){
                 form.on('submit(form)', function(){
                     shopCustomerAccountApi.addShopCustomerAccount($('#addForm'),function(){
                         parent.window.dataReload();
                         Common.info("添加成功");
                         Lib.closeFrame();
                     });
                });
            });
        
            $("#addButton-cancel").click(function(){
                Lib.closeFrame();
            });
        }
    			
    }
    exports('add',view);
});